info:
  name: TestingReset
  type: http
  seq: 1

http:
  method: POST
  url: "{{Host}}v{{version}}/{{controller}}/reset"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
