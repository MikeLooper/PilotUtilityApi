info:
  name: TestingReset
  type: http
  seq: 1

http:
  method: POST
  url: "{{Host}}testing/reset"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
